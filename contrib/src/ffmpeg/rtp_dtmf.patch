diff --git a/libavformat/rtpenc.c b/libavformat/rtpenc.c
index c764fca..9976e7b 100644
--- a/libavformat/rtpenc.c
+++ b/libavformat/rtpenc.c
@@ -304,11 +304,15 @@ void ff_rtp_send_data(AVFormatContext *s1, const uint8_t *buf1, int len, int m)
 #else
     avio_w8(s1->pb, RTP_VERSION << 6);
 #endif
-    avio_w8(s1->pb, (s->payload_type & 0x7f) | ((m & 0x01) << 7));
+    avio_w8(s1->pb, ((s->flags & FF_RTP_FLAG_DTMF) ? 101 : s->payload_type & 0x7f) | ((m & 0x01) << 7));
     avio_wb16(s1->pb, s->seq);
     avio_wb32(s1->pb, s->timestamp);
     avio_wb32(s1->pb, s->ssrc);
 
+    if (s->flags & FF_RTP_FLAG_DTMF) {
+        s->flags &= ~FF_RTP_FLAG_DTMF;
+    }
+
 #ifdef EXT_ABS_SEND_TIME
     avio_wb16(s1->pb, 0xBEDE);      // magic word
     avio_wb16(s1->pb, 0x0001);      // length=1
@@ -496,12 +500,29 @@ static int rtp_send_ilbc(AVFormatContext *s1, const uint8_t *buf, int size)
 static int rtp_write_packet(AVFormatContext *s1, AVPacket *pkt)
 {
     RTPMuxContext *s = s1->priv_data;
-    AVStream *st = s1->streams[0];
-    int rtcp_bytes;
     int size= pkt->size;
 
+    // send dtmf
+    if (pkt->flags & FF_RTP_FLAG_DTMF) {
+        av_log(s1, AV_LOG_DEBUG, "%d: write DTMF len=%d\n", pkt->stream_index, size);
+        int m = 0;
+        if (pkt->flags & FF_RTP_FLAG_FORCE_MARKER) {
+            m = 1;
+        }
+        if (pkt->flags & FF_RTP_FLAG_FORCE_TIMESTAMP) {
+            s->cur_timestamp = s->base_timestamp + pkt->pts;
+        }
+        s->timestamp = s->cur_timestamp;
+        s->flags |= FF_RTP_FLAG_DTMF;
+        ff_rtp_send_data(s1, pkt->data, size, m);
+        return 0;
+    }
+
     av_log(s1, AV_LOG_TRACE, "%d: write len=%d\n", pkt->stream_index, size);
 
+    AVStream *st = s1->streams[0];
+    int rtcp_bytes;
+
     rtcp_bytes = ((s->octet_count - s->last_octet_count) * RTCP_TX_RATIO_NUM) /
         RTCP_TX_RATIO_DEN;
     if ((s->first_packet || ((rtcp_bytes >= RTCP_SR_SIZE) &&
diff --git a/libavformat/rtpenc.h b/libavformat/rtpenc.h
index 70ea85b..95bd0c0 100644
--- a/libavformat/rtpenc.h
+++ b/libavformat/rtpenc.h
@@ -70,6 +70,9 @@ typedef struct RTPMuxContext RTPMuxContext;
 #define FF_RTP_FLAG_SKIP_RTCP 4
 #define FF_RTP_FLAG_H264_MODE0 8
 #define FF_RTP_FLAG_SEND_BYE  16
+#define FF_RTP_FLAG_DTMF 32
+#define FF_RTP_FLAG_FORCE_TIMESTAMP 64
+#define FF_RTP_FLAG_FORCE_MARKER 128
 
 #define FF_RTP_FLAG_OPTS(ctx, fieldname) \
     { "rtpflags", "RTP muxer flags", offsetof(ctx, fieldname), AV_OPT_TYPE_FLAGS, {.i64 = 0}, INT_MIN, INT_MAX, AV_OPT_FLAG_ENCODING_PARAM, "rtpflags" }, \
